apiVersion: v1
kind: Service
metadata:
  name: chat-socket-service
spec:
  selector:
    app: chat-socket-pod
  ports:
  - port: 31000
    nodePort: 31000
    targetPort: chat-s-port
    protocol: TCP
  type: NodePort
